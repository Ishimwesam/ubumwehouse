# Same-origin production deployment where Express serves frontend/dist.
VITE_API_BASE_URL=/api

# If the frontend is hosted separately, use the full backend API URL instead:
# VITE_API_BASE_URL=https://api.your-domain.example/api
